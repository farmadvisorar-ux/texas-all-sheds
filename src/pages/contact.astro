---
import Base from '../layouts/Base.astro';
import site from '../data/site.json';
import { phoneLive, phoneHref, contactName, addressLive, addressLine } from '../lib/contact';

// `||`, not `??`. An unset GitHub Actions variable interpolates as an empty
// string rather than being absent, and `??` only falls through on null and
// undefined — so `""` would win and silently override the real endpoint.
const endpoint = (import.meta.env.PUBLIC_FORM_ENDPOINT || site.formEndpoint || '').trim();

// All three land in the same inbox, but publishing what each one is *for*
// means a warranty question and a price request arrive pre-sorted.
const mailboxes = [
  { address: site.emails.info, purpose: 'General questions, orders in progress, warranty' },
  { address: site.emails.sales, purpose: 'Availability, lead times, dealer and volume enquiries' },
  { address: site.emails.quotes, purpose: 'Pricing and written quotes' },
];
---

<Base
  title="Contact Us"
  description="Get in touch with Texas All Sheds about a steel building project — sizing, pricing, engineering or delivery."
  crumbs={[]}
  current="Contact"
>
  <section class="section">
    <div class="wrap contact-grid">
      <div>
        <p class="eyebrow">Get in touch</p>
        <h1 class="page-title">Contact Us</h1>
        <p class="lede">
          If you already know roughly what you want built, the
          <a href="/quote/">quote form</a> collects the details we need and gets you a written
          number faster. For anything else, this reaches us directly.
        </p>

        <dl class="contact-details">
          {phoneLive && (
            <div>
              <dt>Phone</dt>
              <dd>
                <a href={phoneHref}>{site.phone}</a>
                {contactName && <span class="ask-for">Ask for {contactName}</span>}
              </dd>
            </div>
          )}
          {!site.emailPending && site.emails && (
            <div>
              <dt>Email</dt>
              <dd>
                <ul class="mailboxes">
                  {mailboxes.map((m) => (
                    <li>
                      <a href={`mailto:${m.address}`}>{m.address}</a>
                      <span>{m.purpose}</span>
                    </li>
                  ))}
                </ul>
              </dd>
            </div>
          )}
          <div><dt>Hours</dt><dd>{site.hours}</dd></div>
          {addressLive && (
            <div>
              <dt>Location</dt>
              <dd>{addressLine}</dd>
            </div>
          )}
        </dl>

        {(site.emailPending || !phoneLive || !addressLive) && (
          <div class="admin-note">
            <strong>Publish note — remove before launch.</strong>
            {site.emailPending && (
              <Fragment>
                {' '}The email address <code>{site.email}</code> is reserved but not yet
                receiving. Set up forwarding, then flip <code>emailPending</code> to
                <code>false</code> in <code>src/data/site.json</code> and it appears here,
                in the footer and in the business schema.
              </Fragment>
            )}
            {!addressLive && (
              <Fragment>
                {' '}No postal address is published yet. Fill in <code>address</code> in
                <code>src/data/site.json</code> and it appears here, in the footer and in
                the business schema. Two operations sharing one address are treated as
                duplicate listings whatever their phone numbers, so this one needs its own.
              </Fragment>
            )}
            {!phoneLive && (
              <Fragment>
                {' '}The phone number works the same way through <code>phonePending</code>.
                This business needs a line of its own: Google resolves a Business Profile by
                address plus phone, so a listing sharing a number with another entity is
                filed as a duplicate and suspended.
              </Fragment>
            )}
          </div>
        )}

        <div class="prose" style="margin-top:2rem">
          <h2>What is useful to tell us</h2>
          <ul>
            <li>What the building is for, and what goes inside it</li>
            <li>A rough size, if you have one in mind</li>
            <li>The delivery address — this determines the engineering</li>
            <li>Your timeline</li>
          </ul>
        </div>
      </div>

      <div class="form-wrap">
        {!endpoint && (
          <div class="notice" role="status">
            <strong>Form not yet connected.</strong> Set <code>PUBLIC_FORM_ENDPOINT</code> in
            your environment variables to start receiving messages.
          </div>
        )}

        <form class="contact-form" id="contact-form" method="POST" action={endpoint || undefined} novalidate>
          <div class="field">
            <label for="name">Name <span aria-hidden="true">*</span></label>
            <input id="name" name="name" type="text" required autocomplete="name" />
          </div>
          <div class="field">
            <label for="email">Email <span aria-hidden="true">*</span></label>
            <input id="email" name="email" type="email" required autocomplete="email" />
          </div>
          <div class="field">
            <label for="phone">Phone</label>
            <input id="phone" name="phone" type="tel" autocomplete="tel" />
          </div>
          <div class="field">
            <label for="postal">ZIP / Postal code</label>
            <input id="postal" name="postal" type="text" autocomplete="postal-code" />
          </div>
          <div class="field">
            <label for="message">How can we help? <span aria-hidden="true">*</span></label>
            <textarea id="message" name="message" rows="6" required></textarea>
          </div>

          <input type="hidden" name="_subject" value="New message — texasallsheds.com" />

          <div class="visually-hidden" aria-hidden="true">
            <label for="_gotcha">Leave this blank</label>
            <input id="_gotcha" name="_gotcha" type="text" tabindex="-1" autocomplete="off" />
          </div>

          <button class="btn btn--primary btn--submit" type="submit">Send Message</button>
        </form>
      </div>
    </div>
  </section>
</Base>

<style>
  .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(2rem, 5vw, 4rem); align-items: start; }
  .page-title { font-size: clamp(2rem, 5vw, 3rem); margin-block: 0.5rem 1rem; }
  .lede a { color: var(--accent); }

  .contact-details { margin-top: 2rem; display: grid; gap: 1rem; }
  .contact-details dt { font-size: 0.6875rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-faint); }
  .contact-details dd { margin: 0.125rem 0 0; font-size: 1rem; }
  .contact-details a { color: var(--accent); text-decoration: none; }
  .contact-details a:hover { text-decoration: underline; }

  .mailboxes { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.625rem; }
  .mailboxes li { display: flex; flex-direction: column; }
  .mailboxes span { font-size: 0.8125rem; color: var(--text-faint); line-height: 1.4; }

  .form-wrap { background: var(--bg-raised); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: clamp(1.25rem, 3vw, 2rem); }
  .field { display: flex; flex-direction: column; gap: 0.375rem; margin-bottom: 1rem; }
  .field label { font-size: 0.8125rem; font-weight: 500; }
  .field label span { color: var(--accent); }
  input, textarea {
    padding: 0.625rem 0.75rem; width: 100%;
    background: var(--bg); border: 1px solid var(--border-strong);
    border-radius: var(--radius); font-size: 0.9375rem;
  }
  textarea { resize: vertical; }
  input[aria-invalid='true'], textarea[aria-invalid='true'] { border-color: #dc2626; }
  .err { font-size: 0.75rem; color: #dc2626; }
  .btn--submit { width: 100%; justify-content: center; padding-block: 0.875rem; }

  .notice, .admin-note {
    padding: 0.875rem 1rem; margin-bottom: 1.5rem;
    border-radius: var(--radius); font-size: 0.8125rem; color: var(--text-muted);
  }
  .notice {
    background: color-mix(in srgb, var(--accent) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent);
  }
  .admin-note {
    margin-top: 1.5rem;
    background: color-mix(in srgb, #dc2626 8%, transparent);
    border: 1px dashed color-mix(in srgb, #dc2626 45%, transparent);
  }
  .admin-note strong { display: block; color: #dc2626; margin-bottom: 0.25rem; }
  code { font-family: var(--font-mono); font-size: 0.75rem; background: var(--bg-sunken); padding: 0.0625rem 0.25rem; border-radius: 2px; }

  .form-success {
    padding: 2rem; text-align: center;
    background: color-mix(in srgb, var(--accent) 8%, transparent);
    border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent);
    border-radius: var(--radius-lg);
  }

  .ask-for {
    display: block; margin-top: 0.125rem;
    font-size: 0.8125rem; color: var(--text-faint);
  }

  @media (max-width: 860px) { .contact-grid { grid-template-columns: 1fr; } }
</style>

<script>
  import { wireForm } from '../scripts/form-submit.js';
  wireForm('contact-form', {
    successHeading: 'Thanks — message received.',
    successBody: 'We will reply shortly.',
  });
</script>
